{
  "name": "eureka",
  "scripts": {
    "dokku": {
      "predeploy": "RAILS_ENV=production bundle exec rake assets:precompile bundle exec rake assets:precompile",
      "postdeploy": "bundle exec rake db:migrate"
    }
  }
}
